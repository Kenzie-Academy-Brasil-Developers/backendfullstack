import { IAddContact } from "../../interface/contact.interface"
import { IToken } from "../../interface/user.interface"

const addContactService = async ( data:IAddContact, { id:idUserToken }:IToken ) => {

}

export default addContactService